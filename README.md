# ubuntu24tweak

针对ubuntu24.04做一些优化

#### 待优化点或待处理问题，列在 TODO.md 中，完成后可标记 [x]，并在行末追加 @done(YYYY-MM-DD)。
#### 成果组织 
> 文档、说明类的，单文件，记录在 docs/ 下，以任务目标为文件名。
> 脚本类，单文件，写入 scripts/ 下，以任务目标为文件名。
> 多文件的，均应建立任务目标为名称的文件夹，并将脚本、配置、文档、说明等，存入此文件夹内。